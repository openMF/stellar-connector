apply plugin: 'war'

configurations {
    providedRuntime
}

war {
    baseName = 'stellar-connector'
    version = '0.0.1'
}

dependencies {
    providedRuntime("org.springframework.boot:spring-boot-starter-tomcat")
}
